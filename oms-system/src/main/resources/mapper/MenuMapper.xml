<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iecas.system.mapper.MenuMapper">
    <select id="selectList" parameterType="Long" resultType="com.iecas.system.domain.Menu">
        SELECT m.id, m.menu_name, m.parent_id, m.component, m.menu_type, m.menu_icon FROM sys_menu m
        LEFT JOIN sys_role_menu rm ON m.id = rm.menu_id
        LEFT JOIN sys_user_role ur ON rm.role_id = ur.role_id
        WHERE ur.user_id = #{userId} ORDER BY m.parent_id, m.menu_sort ASC
    </select>
</mapper>
